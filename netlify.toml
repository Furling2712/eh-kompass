# Netlify-Deployment-Konfiguration.
# Vercel erkennt Astro automatisch und benötigt keine eigene Datei.
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
